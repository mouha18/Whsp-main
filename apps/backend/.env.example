# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=whsp
DB_USER=root
DB_PASSWORD=

# Server Configuration
PORT=3001
NODE_ENV=development

# AI Service Configuration
AI_SERVICE_URL=http://localhost:8001
AI_PROCESS_TIMEOUT=300000
AI_RETRY_COUNT=1

# Audio Encryption (Phase 7)
# Generate a key using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# AUDIO_ENCRYPTION_KEY=your-32-byte-hex-key-here

# Audio Deletion (Phase 7)
# Set to true to automatically delete audio files after successful export
DELETE_AUDIO_AFTER_EXPORT=false

# Logging Configuration (Phase 7)
# Options: debug, info, warn, error
LOG_LEVEL=info
